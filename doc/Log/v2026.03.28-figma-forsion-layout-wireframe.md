# v2026.03.28 - Figma 线框：Forsion AI Studio 层级布局

## 内容

根据 `client/App.tsx`、`Sidebar.tsx`、`ChatArea.tsx`、`WorkspacePanel.tsx`、`AgentStatusBar.tsx` 等，在 Figma 中建立与代码一致的主界面层级线框（非高保真视觉稿，侧重信息架构与区域划分）。

## Figma 文件

- **名称**: Forsion AI Studio - Layout Wireframe
- **链接**: https://www.figma.com/design/uQVbWVU2OeSy0S0dIrzKVP
- **实现方式**: Figma MCP `create_new_file` 创建草稿文件后，`use_figma` 写入 Frame 与标注文字。

## 画布结构（与代码对应）

| Figma 层 | 代码 |
|----------|------|
| `App / h-screen flex row` | 根容器 `flex h-screen overflow-hidden` |
| `Sidebar` | `Sidebar`：History、New Chat、会话列表、Archived、底部用户与 CreditBalance、Settings / Log out |
| `Main column` | `flex-1 flex flex-col` |
| `Header h-16` | 顶栏：移动端 Menu、模型下拉、右侧标题 |
| `ChatArea flex-1` | `ChatArea`：空状态或消息列表（头像 + 气泡 + 工具块等） |
| `Input border-t` | 输入区：附件、Deep Thinking、Force Image、Agent、Files；`AgentStatusBar`；表单与发送 |
| `WorkspacePanel overlay` | `WorkspacePanel`：`fixed right`、宽 `w-80`（320px）叠在主栏之上 |

另有一帧 **Code ↔ Layout map**，用文字列出 `App.tsx` 子树及 `SettingsModal`、`AgentConfigPanel` 等叠加层。

## 说明

- 未绑定团队设计系统组件；为快速对齐开发的结构稿，后续可在同一文件或团队库中替换为真实组件与变量。
