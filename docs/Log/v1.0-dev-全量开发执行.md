# v1.0 开发执行记录

**日期**：2026-03-26
**版本**：v1.0
**模式**：Full Auto
**执行范围**：全部 35 Task（8 Epic）

---

## 执行摘要

| 指标 | 数值 |
|------|------|
| 总任务 | 35 |
| 已完成 | 35 |
| P0 任务 | 17 |
| P1 任务 | 18 |
| 新增文件 | 10 |
| 修改文件 | 7 |

## Epic 完成记录

### Epic 1: Skills 市场与作用域管理（T-001~T-006）
- 新建 `SkillsMarket.tsx` 组件：卡片网格、搜索/筛选、安装/卸载、作用域控制
- 修改 `SettingsModal.tsx`：新增 Skills Tab（第 4 个 Tab）
- 作用域持久化：localStorage 存储，`getGloballyEnabledSkillIds()` 导出供 Runtime 使用
- AC 覆盖：AC-101~107, AC-201~205

### Epic 2: Skill 执行可视化（T-007~T-009）
- 重写 `ToolCallBlock.tsx`：三态视觉（执行中/成功/失败）、重试按钮、超时检测、默认折叠/展开
- 新增 `onRetry` 回调、改进动态边框颜色
- AC 覆盖：AC-301~305

### Epic 3: 跨应用数据中枢（T-010~T-015）
- 新建 `migrate-data-hub.ts`：user_data_partitions、app_authorizations、skills_catalog 三张表
- 新建 `dataHubService.ts`：数据分区 CRUD + 授权管理
- 新建 `dataHub.ts` 路由：/api/data-hub/* 端点，含频率限制
- 新建 `calendar.skill.md`：Forsion Calendar Skill（get_events, create_event, update_event）
- 新建 `AuthorizationDialog.tsx`：授权确认弹窗组件
- AC 覆盖：AC-401~402, AC-501~503, AC-601~605

### Epic 4: 多主题风格系统（T-016~T-020）
- 修改 `types.ts`：themePreset 扩展为 5 值联合类型
- 修改 `App.tsx`：bodyClassMap 支持 apple 和 forsion1
- 修改 `index.html`：新增 Apple Glass 和 Forsion Theme 1 CSS 类
- 修改 `SettingsModal.tsx`：5 个主题预设卡片
- AC 覆盖：AC-701~707

### Epic 5: 对话历史云同步（T-021~T-025）
- 新建 `migrate-data-hub.ts`（追加）：chat_sessions、chat_messages 表
- 新建 `chatSync.ts` 路由：会话 CRUD + 消息分页 + 批量迁移端点
- AC 覆盖：AC-801~805

### Epic 6: 新用户引导（T-026~T-028）
- 新建 `OnboardingFlow.tsx`：3 步 Tooltip 引导、快捷 Prompt 建议、步骤持久化
- AC 覆盖：AC-901~906

### Epic 7: 官方 Skills 管理（T-029~T-033）
- 新建 `skillsCatalog.ts` 路由：公开目录 API + Admin CRUD API
- skills_catalog 表已在 migrate-data-hub.ts 中创建
- AC 覆盖：AC-1201~1205

### Epic 8: 统一用户画像与数据同步（T-034~T-035）
- 新建 `userProfileService.ts`：_profile 命名空间、偏好模型追踪
- 数据同步 API 已在 dataHub.ts 的 /sync 端点实现
- AC 覆盖：AC-1001~1003, AC-1101~1104

## 技术假设

| Task | 假设 | 待确认 |
|------|------|--------|
| T-012 | Calendar Skill URL 使用 `{{API_BASE_URL}}` 占位符 | 需在 agentRuntime 中实现替换 |
| T-010 | payload 字段使用 MySQL JSON 类型 | 需确认 MySQL 版本 >= 5.7.8 |
| T-021 | 表结构基于前端类型推断 | 需与实际使用对齐 |
| T-034 | 画像每 10 次对话重算 | 可调整频率 |

## 修正记录

- SettingsModal 的 themePreset prop 从严格联合类型放宽为 `string`，以支持未来新增主题
- AgentConfigPanel 的 themePreset prop 同步放宽为 `string`
- ToolCallBlock 的 themePreset prop 同步放宽为 `string`
- **2026-03-29**：未持久化主题时，默认 `themePreset` 由 Cyber Tech（`default`）改为 Apple Glass（`apple`）；实现为 `App.tsx` 初始 state 与 `ChatArea` 可选 prop 兜底一致为 `apple`（用户或后端已保存的 `theme` / `themePreset` 仍优先）
